<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PicFixPro" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Base starting theme for API < 31 (core-splashscreen backports the splash UI).
         API 31+ splash attributes live in res/values-v31/themes.xml. -->
    <style name="Theme.PicFixPro.Starting" parent="Theme.PicFixPro" />
</resources>
